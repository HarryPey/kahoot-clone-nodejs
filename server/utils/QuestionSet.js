class QuestionSet {
    constructor(question, options, correctAnswer, defaultDurationPerQuestion) {
        this.question = question;
        this.options = options;
        this.correctAnswer = correctAnswer;
        this.defaultDurationPerQuestion = defaultDurationPerQuestion;
    }
}